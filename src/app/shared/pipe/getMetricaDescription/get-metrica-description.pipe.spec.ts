import { GetMetricaDescriptionPipe } from './get-metrica-description.pipe';

describe('GetMetricaDescription', () => {
  it('create an instance', () => {
    const pipe = new GetMetricaDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
