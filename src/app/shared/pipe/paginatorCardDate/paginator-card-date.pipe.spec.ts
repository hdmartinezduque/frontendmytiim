import { PaginatorCardDatePipe } from './paginator-card-date.pipe';

describe('PaginatorCardDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginatorCardDatePipe();
    expect(pipe).toBeTruthy();
  });
});
