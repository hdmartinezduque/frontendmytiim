import { ValidateFechaCumplimientoPipe } from './validate-fecha-cumplimiento.pipe';

describe('ValidateFechaCumplimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateFechaCumplimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
