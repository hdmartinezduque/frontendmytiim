import { ChooseRecognitionPipe } from './choose-recognition.pipe';

describe('ChooseRecognitionPipe', () => {
  it('create an instance', () => {
    const pipe = new ChooseRecognitionPipe();
    expect(pipe).toBeTruthy();
  });
});
